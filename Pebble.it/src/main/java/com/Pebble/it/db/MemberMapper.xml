<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Pebble.it.db.MemberMapper">
	
	<!-- 회원가입 쿼리 -->
	<insert id="register" parameterType="com.Pebble.it.model.MemberDTO">
		INSERT INTO USERS (USER_ID, PASSWORD, NICKNAME, PROFILE_IMG, FAVORITE_SPORT, SPORT_LEVEL)
        VALUES (#{id}, #{pw}, #{name}, #{profileImg}, #{favoriteSport}, #{sportLevel})
	</insert>

	<!-- 로그인 쿼리 -->
	<select id="login" parameterType="com.Pebble.it.model.MemberDTO"
		resultType="com.Pebble.it.model.MemberDTO">
		SELECT * FROM USERS WHERE USER_ID = #{id} AND PASSWORD = #{pw}
	</select>
</mapper>