<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Pebble.it.db.MemberMapper">

	<!-- 회원가입 쿼리 -->
	<insert id="join" parameterType="com.Pebble.it.model.MemberDTO">
		INSERT INTO TB_USER (
		USER_ID,
		PW, NICK, PROFILE_IMG, FAVORITE_SPORT, SPORT_LEVEL, UID_LEVEL,
		JOIN_DT
		) VALUES (
		#{USER_ID}, #{PW}, #{NICK}, #{PROFILE_IMG}, #{FAVORITE_SPORT},
		#{SPORT_LEVEL},
		#{UID_LEVEL}, SYSDATE
		)
	</insert>

	<!-- 로그인 쿼리 -->
	<resultMap id="MemberResultMap"
		type="com.Pebble.it.model.MemberDTO">
		<result property="USER_ID" column="USER_ID" />
		<result property="PW" column="PASSWORD" />
		<result property="NICK" column="NICKNAME" />
		<result property="PROFILE_IMG" column="PROFILE_IMG" />
		<result property="FAVORITE_SPORT" column="FAVORITE_SPORT" />
		<result property="SPORT_LEVEL" column="SPORT_LEVEL" />
		<result property="JOIN_DT" column="JOIN_DT" /> <!-- 가입 날짜 매핑 추가 -->
	</resultMap>

	<select id="login" parameterType="com.Pebble.it.model.MemberDTO"
		resultMap="MemberResultMap">
		SELECT USER_ID, PW, NICK, PROFILE_IMG,FAVORITE_SPORT, SPORT_LEVEL FROM TB_USER WHERE USER_ID = #{USER_ID} AND PW = #{PW}
	</select>

	<!-- 닉네임 중복 확인 -->
	<select id="checkDuplicateNickname" parameterType="string"
		resultType="string">
		SELECT CASE WHEN COUNT(*) > 0 THEN 'Y' ELSE 'N' END AS
		DUPLICATE
		FROM TB_USER
		WHERE NICK = #{NICK}
	</select>

	<!-- 아이디 중복 확인 -->
	<select id="checkDuplicateId" parameterType="string"
		resultType="string">
		SELECT CASE WHEN COUNT(*) > 0 THEN 'Y' ELSE 'N' END AS
		DUPLICATE
		FROM TB_USER
		WHERE USER_ID = #{USER_ID}
	</select>
	
	<!-- 특정 회원 정보 불러오기 -->
	<select id="userInfo" parameterType="com.Pebble.it.model.MemberDTO">
		SELECT NICK, PROFILE_IMG, FAVORITE_SPORT, SPORT_LEVEL FROM TB_USER WHERE USER_ID = #{USER_ID}
	</select>

</mapper>
